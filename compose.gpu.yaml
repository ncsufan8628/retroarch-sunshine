services:
  retroarch-sunshine:
    devices:
      - /dev/dri:/dev/dri
      - /dev/uinput:/dev/uinput
    volumes:
      - /dev/input:/dev/input
      - /run/udev:/run/udev:ro
    group_add:
      - video
      - input
